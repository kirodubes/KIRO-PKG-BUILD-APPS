post_install() {
    echo -e "\n** Fish Tweak Tool installed"
    echo -e "** Launch it from your menu or run: fish-tweak-tool"
    echo -e "** Install a prompt, toggle plugins, pick a theme — no sudo needed"
    echo -e "**\n"
}

post_upgrade() {
    post_install
}
