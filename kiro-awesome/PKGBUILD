# Maintainer: Erik Dubois <erik.dubois@gmail.com>
pkgname=kiro-awesome
_destname="/etc/skel/.config/awesome/"
_licensedir="/usr/share/kiro/licenses/"
pkgver=26.09
pkgrel=01
pkgdesc="Desktop configuration for Kiro"
arch=('any')
url="https://github.com/kirodubes/kiro-awesome"
license=('GPL3')
makedepends=()
conflicts=('edu-awesome-git')
replaces=('edu-awesome-git')
depends=()
provides=(${pkgname})
options=(!strip !emptydirs)
source=(${pkgname}::"git+https://github.com/kirodubes/kiro-awesome.git")
sha256sums=('SKIP')
install='readme.install'
package() {
	install -dm755 ${pkgdir}${_licensedir}${pkgname}
	install -m644  ${srcdir}/${pkgname}/LICENSE ${pkgdir}${_licensedir}${pkgname}

	install -dm755 ${pkgdir}${_destname}
	cp -r  ${srcdir}/${pkgname}${_destname}* ${pkgdir}${_destname}
}
