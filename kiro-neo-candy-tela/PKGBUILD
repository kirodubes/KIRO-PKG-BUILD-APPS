# Maintainer: Erik Dubois <erik.dubois@gmail.com>
pkgname=kiro-neo-candy-tela
pkgver=26.09
pkgrel=01
_destname1="/usr/share/icons/"
pkgdesc="Beautyline icons completed for ArcoLinux + Candy icons + Tela"
arch=('any')
url="https://github.com/kirodubes/kiro-neo-candy-tela.git"
license=('GPL3')
depends=('neo-candy-icons-git')
replaces=('edu-neo-candy-tela-git')
makedepends=('git')
provides=("${pkgname}")
conflicts=('edu-neo-candy-tela-git')
options=(!strip !emptydirs)
source=(${pkgname}::"git+${url}")
sha256sums=('SKIP')
package() {
	install -dm 755 ${pkgdir}${_destname1}
    cp -r ${srcdir}/${pkgname}${_destname1}/* ${pkgdir}${_destname1}
}
